package com.example.CureMap.repository;

import com.example.CureMap.domain.SymptomInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SymptomInfoRepository extends JpaRepository<SymptomInfo, Long> {
}
